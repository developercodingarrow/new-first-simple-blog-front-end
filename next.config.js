/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["3.143.124.47"], // Just the IP address or domain, without http:// or https://
  },
};

module.exports = nextConfig;
